# Broken Submission

This archive is intentionally incomplete.

It is meant to fail ingestion because it does not include:

- `potato-chips-ai-agent.json`
- `main.py`, `main.js`, or `index.js`
- `package.json` with a valid `main` entry
